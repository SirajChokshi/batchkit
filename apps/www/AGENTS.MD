## Guidelines
- Use tailwind classes for styling, avoid Svelte component CSS or global CSS
- Prefer static HTML with small JS bundles over client-side components for small interactions

## Commands
- run `bun astro sync` to sync the content with the `www/src/content` directory and fix common type issues