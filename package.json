{
  "name": "batchkit-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter batchkit build && bun run --filter batchkit-devtools build && bun run --filter batchkit-devtools-react --filter batchkit-devtools-svelte build && bun run --filter www build",
    "dev": "bun run --filter www dev",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "typescript": "5.3.3"
  }
}
